export interface ISEO {
  title?: string; // do campo seo.title (GROQSeoQuery mapeia para settings.title também; ajuste se quiser)
  description?: string; // do campo seo.description (mapeado para settings.description)
}
